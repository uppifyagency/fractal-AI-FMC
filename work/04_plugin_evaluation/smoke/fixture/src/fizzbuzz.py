"""FizzBuzz — to be implemented by the fractal-coding-loop plugin.

Specification:
- fizzbuzz(n: int) -> str
- Returns "Fizz"     for multiples of 3 (but not 5)
- Returns "Buzz"     for multiples of 5 (but not 3)
- Returns "FizzBuzz" for multiples of BOTH 3 and 5
- Returns str(n)     for any other positive integer
- Raises ValueError  for n <= 0

The walker(s) of the plugin must implement this function. The 5 tests in
tests/test_fizzbuzz.py provide objective acceptance criteria.
"""

# TODO: implement
def fizzbuzz(n: int) -> str:
    raise NotImplementedError("fizzbuzz: to be implemented")
